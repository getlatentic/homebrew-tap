# getlatentic Homebrew tap

```sh
brew install --cask getlatentic/tap/compose
```

[Compose](https://github.com/getlatentic/compose) — the local-first AI Markdown
editor for macOS. Signed, notarized, self-updating (Apple Silicon gets a lean
arm64 build; Intel gets the universal build).
